---
{% if jobconf_sentrydsn is defined %}
sentry_dsn: "{{ jobconf_sentrydsn }}"
{% endif %}

statsd:
    host: {{ jobconf_statsd.host }}
    port: {{ jobconf_statsd.port }}
    prefix: {{ jobconf_statsd.prefix }}

jcaas:
    tools: {{ jobconf_directory }}/tool_destinations.yaml
    dests: {{ jobconf_directory }}/destination_specifications.yaml
